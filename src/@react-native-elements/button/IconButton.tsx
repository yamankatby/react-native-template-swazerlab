import React from 'react';
import { Text, View } from 'react-native';

const IconButton = () => {
	return (
		<View>
			<Text>Hi, IconButton Component!</Text>
		</View>
	);
};

export default IconButton;
